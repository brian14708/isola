# Getting Started

## Local Development

From the repository root:

```bash
nix develop
just build
just run
```

## Build Documentation

Build the static docs site:

```bash
just docs
```

Serve docs locally with live reload:

```bash
just docs-serve
```
