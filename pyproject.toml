[project]
name = "promptkit"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.uv.sources]
promptkit-py = { path = "wasm/crates/python/bundled" }

[dependency-groups]
dev = [
    "ruff>=0.11.2",
]
