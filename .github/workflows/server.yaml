name: Server (Rust)

on:
  push:
    branches:
      - main
      - renovate/**
    paths:
      - .github/**
      - crates/**
      - apis/**
      - Cargo.lock
      - Cargo.toml
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout your repository using git
        uses: actions/checkout@v4
      - name: Install packages
        run: sudo apt-get install -y liblzma-dev protobuf-compiler
      - uses: Swatinem/rust-cache@v2
      - name: Build
        run: cargo build --verbose
      - name: Run tests
        run: cargo test --verbose
