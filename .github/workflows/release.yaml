name: Release

on:
  workflow_dispatch:
  schedule:
    - cron: "30 4 * * 2"
  release:
    types: [created]

permissions: {}

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  GH_REPO: ${{ github.repository }}

jobs:
  targets:
    name: Tag
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      release_tag: ${{ steps.resolve.outputs.release_tag }}
    steps:
      - name: Resolve release tag
        id: resolve
        env:
          RELEASE_TAG: ${{ github.event.release.tag_name || '' }}
        run: |
          if [ "${GITHUB_EVENT_NAME}" = "release" ] && [ -n "${RELEASE_TAG}" ]; then
            tag="${RELEASE_TAG}"
          else
            tag="latest"
          fi

          echo "release_tag=$tag" >> "$GITHUB_OUTPUT"
      - name: Move latest tag to current commit
        if: steps.resolve.outputs.release_tag == 'latest'
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          if ! gh api -X PATCH "repos/$GH_REPO/git/refs/tags/latest" \
            -f sha="$GITHUB_SHA" \
            -F force=true >/dev/null 2>&1; then
            gh api -X POST "repos/$GH_REPO/git/refs" \
              -f ref="refs/tags/latest" \
              -f sha="$GITHUB_SHA" >/dev/null
          fi
  oci:
    name: OCI
    needs: [targets, python-runtime, js-runtime]
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout your repository using git
        uses: actions/checkout@v6
      - name: Install Nix
        uses: cachix/install-nix-action@v31
      - name: Setup Cachix
        uses: cachix/cachix-action@v16
        with:
          name: isola
          authToken: "${{ secrets.CACHIX_AUTH_TOKEN }}"
      - name: Build container image
        run: |
          nix --accept-flake-config build .#packages.x86_64-linux.oci
          ./result | docker load
      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Tag and push image
        run: |
          docker tag isola:latest ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.targets.outputs.release_tag }}
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.targets.outputs.release_tag }}

  python-runtime:
    name: Python Runtime
    needs: targets
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout your repository using git
        uses: actions/checkout@v6
      - name: Install Nix
        uses: cachix/install-nix-action@v31
      - name: Setup Cachix
        uses: cachix/cachix-action@v16
        with:
          name: isola
          authToken: "${{ secrets.CACHIX_AUTH_TOKEN }}"
      - name: Build Python package
        run: nix --accept-flake-config build .#python
      - name: Package release artifacts
        run: |
          mkdir -p dist
          tar -C result -czf dist/isola-python-runtime.tar.gz bin lib
      - name: Upload artifacts to target release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "${{ needs.targets.outputs.release_tag }}" -R "$GH_REPO" \
            dist/isola-python-runtime.tar.gz \
            --clobber

  js-runtime:
    name: JS Runtime
    needs: targets
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout your repository using git
        uses: actions/checkout@v6
      - name: Install Nix
        uses: cachix/install-nix-action@v31
      - name: Setup Cachix
        uses: cachix/cachix-action@v16
        with:
          name: isola
          authToken: "${{ secrets.CACHIX_AUTH_TOKEN }}"
      - name: Build JS package
        run: nix --accept-flake-config build .#js
      - name: Package release artifacts
        run: |
          tar -C result -czf isola-js-runtime.tar.gz --transform='s,^,isola-js-runtime/,' bin
      - name: Upload artifacts to target release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "${{ needs.targets.outputs.release_tag }}" -R "$GH_REPO" \
            isola-js-runtime.tar.gz \
            --clobber

  python-sdk:
    name: Python SDK (${{ matrix.os }})
    needs: targets
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout your repository using git
        uses: actions/checkout@v6
      - name: Build Python SDK artifacts
        uses: PyO3/maturin-action@v1
        with:
          command: build
          args: --release --manifest-path crates/python-sdk/Cargo.toml --out dist
      - name: Setup uv
        uses: astral-sh/setup-uv@v6
      - name: Publish
        if: needs.targets.outputs.release_tag != 'latest'
        run: |
          uv publish dist/*

  cargo_publish:
    name: Cargo
    needs: targets
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout your repository using git
        uses: actions/checkout@v6
      - uses: actions-rust-lang/setup-rust-toolchain@v1
      - name: Dry run crates publish to crates.io
        if: needs.targets.outputs.release_tag == 'latest'
        run: |
          cargo publish --locked --dry-run
      - name: Publish crates to crates.io
        if: needs.targets.outputs.release_tag != 'latest'
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          cargo publish --locked
